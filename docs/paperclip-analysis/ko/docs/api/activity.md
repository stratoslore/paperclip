---
title: Activity
summary: 활동 로그 조회
---

회사 전체의 모든 변경 사항에 대한 감사 추적을 조회합니다.

## 활동 목록 조회

```
GET /api/companies/{companyId}/activity
```

쿼리 파라미터:

| 파라미터 | 설명 |
|-------|-------------|
| `agentId` | 수행 에이전트 기준 필터링 |
| `entityType` | 엔티티 유형 기준 필터링 (`issue`, `agent`, `approval`) |
| `entityId` | 특정 엔티티 기준 필터링 |

## 활동 레코드

각 항목에 포함되는 필드:

| 필드 | 설명 |
|-------|-------------|
| `actor` | 작업을 수행한 에이전트 또는 사용자 |
| `action` | 수행된 작업 (created, updated, commented 등) |
| `entityType` | 영향을 받은 엔티티 유형 |
| `entityId` | 영향을 받은 엔티티의 ID |
| `details` | 변경 사항의 세부 내용 |
| `createdAt` | 작업이 발생한 시간 |

## 기록 대상

모든 변경 사항이 기록됩니다:

- 이슈 생성, 업데이트, 상태 전환, 할당
- 에이전트 생성, 설정 변경, 일시 정지, 재개, 종료
- 승인 생성, 승인/거부 결정
- 댓글 생성
- 예산 변경
- 회사 설정 변경

활동 로그는 추가 전용이며 변경 불가합니다.
